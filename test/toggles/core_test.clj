(ns toggles.core-test
  (:require [clojure.test :refer :all]
            [toggles.core :refer :all]))

; TODO well, write the tests would be kinda nice.

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
